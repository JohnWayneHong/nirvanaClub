<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="data"
            type="com.jgw.delingha.sql.entity.BaseCodeEntity" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/white"
        android:paddingLeft="15dp"
        android:paddingRight="15dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{data.getCode}"
                android:textColor="@color/gray_32"
                android:textSize="14sp"
                tools:text="39306382674993903207" />

            <TextView
                android:layout_width="30dp"
                android:layout_height="20dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/item_edittext_background"
                android:gravity="center"
                android:text="@{data.codeLevelName}"
                android:textColor="#D9D9D9"
                android:visibility="@{data.codeLevelNameVisible}"
                tools:text="盒" />

        </LinearLayout>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|end"
            android:layout_marginRight="60dp"
            android:text="@{data.getCodeStatusText}"
            android:textColor="@{data.getCodeStatusTextColor}"
            android:textSize="12sp"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            tools:text="@string/pda_scan_checking"
            tools:visibility="visible" />
        <RelativeLayout
            android:id="@+id/rl_common_scan_back_delete"
            android:layout_width="50dp"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:scaleType="center"
                android:src="@drawable/icon_clear" />

        </RelativeLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="0.5px"
            android:layout_alignParentBottom="true"
            android:background="@color/gray_dd" />
    </RelativeLayout>
</layout>